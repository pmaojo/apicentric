
name: dummy
version: "1.0"
description: "Dummy service for GUI"
server:
  port: 8080
  base_path: "/api"
endpoints:
  - method: GET
    path: "/health"
    responses:
      200:
        content_type: "application/json"
        body: '{"status": "ok"}'
